{
  "name": "my-todo-app",
  "version": "1.0.0",
  "description": "A full-stack To-Do application with React and Express",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev -w apps/frontend\" \"npm run dev -w apps/backend\"",
    "build": "npm run build -w apps/frontend && npm run build -w apps/backend",
    "clean": "rm -rf **/node_modules **/dist **/build",
    "prepare": "npm run prepare -w apps/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
